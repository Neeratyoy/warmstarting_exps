Main experiment configs:

* Scaling number of heads keeping head size fixed: `width_num_heads/dev` and `width_num_heads/toy`
* Scaling head dimension keeping num heads fixed: `width_head_size/dev` and `width_head_size/toy`
